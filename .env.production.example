# StorAI-Booker Production Environment Configuration
# Copy this file to .env.production and fill in your actual values

# ============================================================================
# MinIO / Object Storage
# ============================================================================
# SECURITY: Change these default credentials in production!
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme_random_password_here

# ============================================================================
# LLM Provider Configuration
# ============================================================================
# Required: At least one provider API key must be set

# Google AI (Gemini) - Recommended for cost-effectiveness
GOOGLE_API_KEY=your_google_api_key_here

# OpenAI (GPT-4, DALL-E) - Optional
OPENAI_API_KEY=

# Anthropic (Claude) - Optional
ANTHROPIC_API_KEY=

# Default provider to use (google|openai|anthropic)
DEFAULT_LLM_PROVIDER=google

# Default models
DEFAULT_TEXT_MODEL=gemini-2.5-flash
DEFAULT_IMAGE_MODEL=gemini-2.5-flash-image

# ============================================================================
# Application Configuration
# ============================================================================
APP_ENV=production
LOG_LEVEL=info

# ============================================================================
# Security (REQUIRED - change these in production!)
# ============================================================================
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY=your_jwt_secret_key_here_change_this
SECRET_KEY=your_app_secret_key_here_change_this

# ============================================================================
# External Access (if deploying to cloud/remote server)
# ============================================================================
# If deploying to a remote server, set your domain/IP here
# Example: EXTERNAL_URL=https://storai.yourdomain.com
# EXTERNAL_URL=http://localhost

# ============================================================================
# Optional: Email/Notifications (Phase 6)
# ============================================================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# SMTP_FROM=noreply@yourdomain.com

# ============================================================================
# Optional: Monitoring (Phase 6)
# ============================================================================
# SENTRY_DSN=
# PROMETHEUS_ENABLED=false

# ============================================================================
# Notes
# ============================================================================
# 1. Never commit .env.production to version control
# 2. Keep this file as a template only
# 3. Use strong passwords for MINIO in production
# 4. Restrict API keys to specific domains/IPs if possible
# 5. Consider using secrets management (AWS Secrets Manager, HashiCorp Vault, etc.)
