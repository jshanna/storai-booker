# StorAI-Booker Environment Configuration
# Copy this file to .env.production and fill in your actual values
#
# Quick Start:
#   cp .env.production.example .env.production
#   # Edit .env.production with your values
#   docker compose up -d

# ============================================================================
# REQUIRED: MinIO / Object Storage
# ============================================================================
# SECURITY: Change these default credentials!
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme_use_openssl_rand_base64_32

# ============================================================================
# REQUIRED: Security Keys
# ============================================================================
# Generate with: openssl rand -hex 32
SECRET_KEY=changeme_generate_with_openssl_rand_hex_32
JWT_SECRET_KEY=changeme_generate_with_openssl_rand_hex_32

# ============================================================================
# RECOMMENDED: LLM Provider Configuration
# ============================================================================
# At least one provider API key is needed for story generation
# Users can also configure API keys in the Settings UI

# Google AI (Gemini) - Recommended for cost-effectiveness
# Get your key at: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=

# OpenAI (GPT-4, DALL-E) - Optional
OPENAI_API_KEY=

# Anthropic (Claude) - Optional
ANTHROPIC_API_KEY=

# Default provider to use (google|openai|anthropic)
DEFAULT_LLM_PROVIDER=google
DEFAULT_TEXT_MODEL=gemini-2.5-flash
DEFAULT_IMAGE_MODEL=gemini-2.0-flash-exp

# ============================================================================
# OPTIONAL: OAuth Authentication
# ============================================================================
# Enable Google and/or GitHub login for users

# Google OAuth
# Setup: https://console.cloud.google.com/ -> APIs & Services -> Credentials
# Redirect URI: http://localhost/api/auth/google/callback (or your domain)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth
# Setup: https://github.com/settings/developers -> OAuth Apps
# Callback URL: http://localhost/api/auth/github/callback (or your domain)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# OAuth redirect base URL (your frontend URL)
OAUTH_REDIRECT_BASE_URL=http://localhost

# ============================================================================
# OPTIONAL: Sentry Error Tracking
# ============================================================================
# Create free account at: https://sentry.io
# Create projects for Python (backend) and React (frontend)

# Backend Sentry
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=0.1

# Frontend Sentry
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=production

# ============================================================================
# OPTIONAL: Application Settings
# ============================================================================
ENV=production
DEBUG=false
LOG_LEVEL=info
LOG_FORMAT=json

# JWT token expiration (minutes)
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# ============================================================================
# OPTIONAL: External Access (for cloud/remote deployment)
# ============================================================================
# If deploying to a remote server, update these:
# OAUTH_REDIRECT_BASE_URL=https://storai.yourdomain.com
# CORS_ORIGINS=["https://storai.yourdomain.com"]

# ============================================================================
# Security Notes
# ============================================================================
# 1. NEVER commit .env.production to version control
# 2. Use strong, unique passwords (openssl rand -base64 32)
# 3. Rotate API keys and secrets periodically
# 4. Restrict API keys to specific domains if possible
# 5. Consider secrets management for production (AWS Secrets Manager, Vault)
